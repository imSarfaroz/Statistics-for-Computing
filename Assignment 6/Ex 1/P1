# Problem Set #6
# Sarfaroz Yunusov
# Ex 1 (Part1)

import pylab
import math


def meanOfList(list):
    sum = 0
    for i in list:
        sum = sum + i
    
    mean = sum/len(list)
    return mean

def squaredDeviation(list):
    # finding  the mean
    sum = 0
    for i in list:
        sum = sum + i  # the sum of all int numbers in the list
        
    mean = sum/len(list)  # the dev of sum of ints in list and the lengt of the list
    #print(mean)
    
    devs = 0        #deviation
    for j in list:
        devs = devs + (j - mean) ** 2  # the sum of squared deviation score

    return devs # returning the sum of squared dev using the definitial formula

def standardDeviation(list):
    return math.sqrt(squaredDeviation(list) / (len(list) - 1))

def statistics(list1, list2):
    print("Mean of the first List1: %.2f"%meanOfList(list1))
    print("Mean of the first List2: %.2f\n"%meanOfList(list2))
    
    print("The Length of the List1: %d"%len(list1))
    print("The Length of the List2: %d\n"%len(list2))   
    
    print("The standard deviation of the List1: %.2f"%standardDeviation(list1))
    print("The standard deviation of the List2: %.2f"%standardDeviation(list2))
    
    


statistics([64, 68, 84, 65, 50, 60, 81, 76],
           [53, 44, 61, 50, 40, 39, 58, 42])
